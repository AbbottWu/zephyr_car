description: Light Sensor On I2C bus

compatible: "light_sensor"

on-bus: i2c

properties:
  reg:
    type: int
    required: true
    description: |
      The I2C address of the light sensor.
  label:
    type: string
    description: |
      A label for the device.
